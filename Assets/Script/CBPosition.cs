using UnityEngine;
using System.Collections;

public class CBPosition : MonoBehaviour {

	// Use this for initialization
	void Start () {
        
    }
	
	// Update is called once per frame
	void Update () {
        transform.localPosition = new Vector3(600, 0, 0);
    }
}
